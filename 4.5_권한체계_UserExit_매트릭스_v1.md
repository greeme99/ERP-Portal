# 4.5 권한체계 · User Exit 매트릭스 (v1)

> **문서 목적**: AX-ERP Portal 의 권한그룹(역할) 체계와 User Exit 확장 훅의 관계를 정리한다.
> admin 이 권한 설계를 검토할 때, 그리고 **사용자 메뉴얼을 작성할 때** 참조하는 기준 문서다.
>
> 화면에서 동일한 내용을 실시간으로 확인할 수 있다 — **13. 공통/플랫폼 → User Exit 관리 (COM-27)**.
> 그 화면은 `permStore`(COM-02 권한 매트릭스) 설정을 그대로 반영하므로, 권한을 바꾸면 표도 함께 바뀐다.
> 이 문서는 **기본 설정 기준 스냅샷**이며, 운영 중 변경된 권한은 COM-27 화면이 정본이다.

---

## 1. 권한 체계

### 1.1 권한그룹 (역할) — 9종

| 권한그룹 | 성격 | 담당 모듈 (기본 승인) |
| :--- | :--- | :--- |
| **관리자** | **admin** — 전 모듈 승인 | 전체 |
| 경영진 | 전 모듈 조회 전용 | 없음 (전체 조회) |
| 영업 | 기능 역할 | 01. 영업관리 (sd) |
| 구매 | 기능 역할 | 03. 구매관리 (mm) |
| 생산 | 기능 역할 | 02. SCM (scm), 05. 생산관리 (pp) |
| 품질 | 기능 역할 | 06. 품질관리 (qm) |
| 물류 | 기능 역할 | 04. 물류관리 (le) |
| 회계 | 기능 역할 | 08. 재무회계 (fi), 09. 관리회계 (co) |
| 연구소 | 기능 역할 | 07. 연구개발 (plm) |

- `role === "관리자"` 를 **admin** 으로 판정한다. admin 은 권한 매트릭스와 무관하게 전 모듈 승인이다.
- 기능 역할은 담당 모듈에 **승인**, 그 외 모듈에 **조회** 가 기본값이다.
- `status !== "활성"` 인 사용자(퇴사자 등)는 **모든 모듈이 "없음"** 이다. 역할과 무관하게 차단된다.

### 1.2 권한 등급 — 4단계

```
없음  <  조회  <  편집  <  승인
```

상위 등급은 하위를 포함한다. 등급별로 허용되는 동작은 다음과 같다.

| 등급 | 메뉴 표시 | 화면 진입 | 생성·수정·업로드 | 삭제·승인 |
| :--- | :---: | :---: | :---: | :---: |
| 없음 | ✕ | ✕ | ✕ | ✕ |
| 조회 | ○ | ○ | ✕ | ✕ |
| 편집 | ○ | ○ | ○ | ✕ |
| 승인 | ○ | ○ | ○ | ○ |

### 1.3 인가 집행 지점

| 지점 | 구현 | 비고 |
| :--- | :--- | :--- |
| 메뉴 숨김 | `Gnb.tsx` — 권한 "없음" 모듈을 GNB 에서 제외 | |
| 라우트 차단 | `ModuleGuard.tsx` — 주소창 직접 진입도 차단 | `ROUTE_GUARD_ENABLED` 로 끌 수 있다 |
| 액션 차단 | `MasterCrudPage` · `MassUpdateBar` · 문서 화면 | 버튼 비활성 + 이유 툴팁 |
| 서버측 재검증 | `server/authz.mjs` — 쓰기 요청 403 | 저장 키 → 모듈 매핑으로 판정 |

> **보안 한계 (반드시 인지)**
> 이 프로토타입은 **인증이 없다**. 사용자 전환은 화면에서 선택하는 방식이며 비밀번호를 다루지 않는다.
> 서버는 요청 헤더(`X-ERP-User-Role` 등)로 신원을 받으므로 **위조가 가능하다.**
> 따라서 서버측 재검증은 암호학적 보안 경계가 아니라 **클라이언트 우회로 인한 오조작 방지·감사** 목적이다.
> 운영 전환 시 세션/토큰 기반 인증으로 교체해야 한다.

---

## 2. User Exit (확장 훅)

### 2.1 개념

표준 로직 사이에 고객 정의 코드를 끼우는 지점이다(SAP User Exit 개념).
**검증은 모든 사용자에게 항상 실행된다.** 각 Exit 은 `approval` 로 "규칙 위반을
예외 승인할 수 있는 권한"을 선언하며, 그 권한이 있으면 위반을 경고로 통과시킬 수 있다.

```
표준 로직 → [User Exit 지점: 항상 실행] → 표준 로직 계속
                  ↓
        규칙 위반?  → 아니오: 통과
                    → 예 + 예외승인 권한 있음: 경고 후 통과
                    → 예 + 예외승인 권한 없음: 차단
```

판정 로직은 `shared/businessRules.mjs` 의 순수 함수이며 **서버가 같은 파일을 import** 한다.

- Exit 이 하나도 없으면 **기존과 같은 결과**가 나온다(하위 호환).
- Exit 이 예외를 던지면 **차단**한다. 조용히 넘기면 잘못된 저장이 남기 때문이다.
- Exit 은 코드로 등록한다 — `erp-portal/src/services/userExitDefaults.ts`.

### 2.2 기본 제공 Exit — 3종

| Exit ID | 명칭 | 훅 지점 | 예외승인 권한 | 서버 재검증 | 판정 |
| :--- | :--- | :--- | :--- | :---: | :--- |
| `EXIT_SD_CREDIT_CHECK` | 여신한도 검사 | SD 수주 저장 전 | 재무회계(fi) **승인** | ✕ | 한도 초과 → 차단 (fi 승인자는 경고 통과) / 소진율 90%↑ → 경고 |
| `EXIT_MM_PO_LIMIT` | 발주 승인한도 검사 | MM 발주 저장 전 | 구매관리(mm) **승인** | **○** | 1억원 초과 → 차단 (mm 승인자는 경고 통과) |
| `EXIT_FI_JV_BALANCE` | 전표 차대 평형 검사 | FI 전표 저장 전 | **없음 (예외 불가)** | **○** | 차변≠대변 → 차단, 누구도 넘길 수 없다 |

- **예외승인 권한**은 규칙 위반을 넘길 수 있는 권한이다. 검증 자체는 모든 사용자에게 실행된다.
- 전표 차대는 회계 불변식이라 `approval` 을 두지 않았다 — 관리자도 넘길 수 없다.
- **서버 재검증 ✕** 는 자기완결이 아닌 규칙이다(여신한도는 고객 여신 데이터 조회 필요 → §2.4 T-4).

### 2.3 권한그룹 × 예외승인 매트릭스 (기본 설정 기준)

**검증은 모든 역할에서 실행된다.** 아래 표는 규칙 위반을 **예외 승인**할 수 있는지를 보여준다.

| 권한그룹 | 여신한도<br/>(fi 승인) | 발주 한도<br/>(mm 승인) | 전표 차대<br/>(예외 불가) |
| :--- | :---: | :---: | :---: |
| **관리자** (admin) | 승인가능 | 승인가능 | 불가 |
| 경영진 | 불가 (조회) | 불가 (조회) | 불가 |
| 영업 | 불가 (조회) | 불가 (조회) | 불가 |
| 구매 | 불가 (조회) | **승인가능** | 불가 |
| 생산 | 불가 (조회) | 불가 (조회) | 불가 |
| 품질 | 불가 (조회) | 불가 (조회) | 불가 |
| 물류 | 불가 (조회) | 불가 (조회) | 불가 |
| 회계 | **승인가능** | 불가 (조회) | 불가 |
| 연구소 | 불가 (조회) | 불가 (조회) | 불가 |

**읽는 방법**
- `불가` 는 검증이 안 돈다는 뜻이 **아니다**. 규칙을 위반하면 차단되고 넘길 수 없다는 뜻이다.
- 전표 차대는 `approval` 이 없어 관리자도 `불가` 다 — 회계 불변식이라 의도적이다.
- COM-02 에서 권한을 올리면 해당 역할이 예외승인 가능으로 바뀐다.

### 2.4 설계 트레이드오프 (T-1·T-4 해소 완료)

#### T-1. 권한 기반 바이패스 → **해소** (v1.2)

**이전 설계**: `requires` 를 충족하지 못하면 Exit 자체를 건너뛰었다. 그 결과 권한이 낮은
사용자에게 검증이 오히려 느슨해져 통제가 거꾸로 작동했다(영업이 여신한도 초과 수주를 통과).

**현재 설계**: 검증은 **항상 실행**하고, `approval` 권한은 **규칙 위반을 예외 승인**할 때만 쓴다.
일반적인 ERP 통제 방식이며 권한이 높을수록 예외를 허용한다.

| 사용자 | fi 권한 | 여신한도 초과 수주 |
| :--- | :--- | :--- |
| 영업 | 조회 | **차단** (예외승인 권한 없음) |
| 회계 | 승인 | 경고 후 통과 |
| 관리자 | 승인 | 경고 후 통과 |

#### T-2. 예외 승인 경로 → **추가** (v1.2)

승인 권한자는 규칙 위반을 경고로 통과시킬 수 있다(`⚠️` 메시지 + 확인). 승인 권한이 없으면
차단되며, 어떤 권한이 있으면 넘길 수 있는지 콘솔과 메시지로 알려준다.
단 전표 차대는 `approval` 을 두지 않아 **누구도 넘길 수 없다.**

#### T-3. Exit 오류는 차단으로 처리한다 (fail-closed · 유지)

Exit 이 예외를 던지면 저장을 중단한다. 조용히 넘기면 검증되지 않은 문서가 남기 때문이다.
대가는 **Exit 하나의 버그가 해당 업무를 멈춘다**는 것이다. Exit 배포 전 검증이 필요하다.

#### T-4. Exit 이 화면 전용 → **부분 해소** (v1.2)

**자기완결 규칙**(문서 자신과 상수로만 판정되는 규칙)은 서버가 재검증한다.
규칙 함수는 `shared/businessRules.mjs` 한 파일이며 **클라이언트와 서버가 같은 파일을 import** 한다
(중복 구현으로 판정이 어긋나는 것을 막는다).

| 규칙 | 자기완결 | 서버 재검증 | 우회 가능성 |
| :--- | :---: | :---: | :--- |
| 전표 차대 평형 | ○ | **○** | 없음 — API 직접 호출도 422 |
| 발주 승인한도 | ○ | **○** | 없음 — 승인 권한 헤더까지 서버가 판정 |
| 여신한도 | ✕ | ✕ | **남아 있음** — 클라이언트 전용 |

```
UI 경유            → 권한 ○  자기완결 규칙 ○  여신한도 ○
REST API 직접 호출 → 권한 ○  자기완결 규칙 ○  여신한도 ✕   ← 남은 갭
```

**여신한도를 제외한 이유**: 판정에 고객 여신 데이터(다른 저장 키)가 필요해 서버가 문서 의미와
키 관계를 알아야 한다. 프로토타입 범위를 넘어 제외했다.

> **남은 갭**: 여신한도는 UI 를 거치지 않으면 적용되지 않는다. 서버로 옮기려면 저장 키 간
> 참조를 서버가 이해해야 하며, 이는 REST 커넥터를 실 DB 로 전환할 때 함께 다룰 사안이다.

**구현 형식 제약**: 서버(`server/*.mjs`)는 빌드 단계가 없어 `.ts` 를 import 할 수 없다.
그래서 공유 규칙을 `.mjs` + JSDoc 으로 두고 `tsconfig` 에 `allowJs` 와 `shared` 를 추가했다.
새 빌드 도구나 의존성은 추가하지 않았다.

### 2.5 새 Exit 추가 절차

1. `services/userExit.ts` 의 `UserExitPoint` 에 훅 지점 문자열을 추가한다(새 지점이 필요한 경우).
2. `services/userExitDefaults.ts` 에 `registerUserExit({ id, label, point, requires, run })` 를 추가한다.
3. 표준 로직(화면 저장 핸들러)에서 해당 지점에 `runUserExits(point, ctx, authz.can)` 를 호출한다.
4. COM-27 화면과 이 문서의 매트릭스를 갱신한다.

---

## 3. 사용자 메뉴얼 작성 시 반영 사항

메뉴얼에는 다음을 포함한다.

1. **로그인·사용자 전환** — 하단 상태바에서 사용자를 선택한다. 비밀번호는 사용하지 않는다(프로토타입).
   배지로 `admin` / `user` / `비활성` 을 구분한다.
2. **권한이 없을 때 보이는 화면** — 메뉴에 나타나지 않으며, 주소로 직접 접근하면
   🔒 안내 화면이 뜨고 현재 보유 등급과 문의 경로(COM-02)를 알려준다.
3. **버튼이 비활성인 이유** — 마우스를 올리면 필요한 권한을 툴팁으로 안내한다.
   예: "삭제는 승인 권한이 필요합니다."
4. **저장 시 나타나는 검증 메시지** — User Exit 이 낸 메시지다. `⛔` 는 차단, `⚠️` 는 경고(계속 가능),
   `✅` 는 통과 알림이다.
5. **관리자 업무** — COM-01 사용자 관리, COM-02 권한 매트릭스, COM-27 User Exit 관리.
   권한 변경은 즉시 반영되며 재로그인이 필요 없다.
6. **규칙 위반과 예외 승인** — 검증은 모든 사용자에게 적용된다. 규칙을 위반하면 차단되며,
   예외승인 권한이 있는 사용자만 경고를 확인하고 진행할 수 있다(§2.4 T-1·T-2).
   누가 넘길 수 있는지는 COM-27 예외승인 매트릭스에서 확인한다.
   전표 차대 평형은 예외가 없다는 점을 메뉴얼에 명시한다.
7. **주의** — 서버 연동(REST) 모드에서는 권한 변경이 모든 클라이언트에 공유된다.
   로컬 저장 모드에서는 해당 브라우저에만 적용된다. 상태바 배지로 모드를 확인할 수 있다.

---

## 4. 관련 산출물

| 구분 | 위치 |
| :--- | :--- |
| 권한 판정 로직 | `erp-portal/src/services/authz.ts` |
| 세션(사용자 전환) | `erp-portal/src/services/session.ts` |
| **업무 규칙 (공유)** | **`erp-portal/shared/businessRules.mjs`** — 클라이언트·서버 공용 |
| User Exit 프레임워크 | `erp-portal/src/services/userExit.ts` |
| 기본 Exit 구현 | `erp-portal/src/services/userExitDefaults.ts` |
| 라우트 차단 | `erp-portal/src/components/layout/ModuleGuard.tsx` |
| 서버측 재검증 | `erp-portal/server/authz.mjs` |
| 검증 시나리오 | `erp-portal/scripts/authz-scenario.ts` |
| 화면 | COM-01 사용자 관리 · COM-02 권한 매트릭스 · **COM-27 User Exit 관리** |

## 5. 변경 이력

| 버전 | 일자 | 내용 |
| :--- | :--- | :--- |
| v1.0 | 2026-08-13 | 최초 작성 — 권한 체계, Exit 3종, 매트릭스, 메뉴얼 반영 사항 |
| v1.1 | 2026-08-13 | §2.4 설계 트레이드오프 추가 (T-1~T-4). T-1·T-4 는 미결로 표시 |
| v1.2 | 2026-08-13 | T-1 해소(검증 항상 실행 + 예외승인만 권한) · T-2 예외 승인 경로 추가 · T-4 부분 해소(자기완결 규칙 서버 재검증). §2.1~2.3 을 새 의미로 갱신 |

> 작성 기준일: 2026-08-13 · 문서 버전 **v1.2**
