# 4.5 권한체계 · User Exit 매트릭스 (v1)

> **문서 목적**: AX-ERP Portal 의 권한그룹(역할) 체계와 User Exit 확장 훅의 관계를 정리한다.
> admin 이 권한 설계를 검토할 때, 그리고 **사용자 메뉴얼을 작성할 때** 참조하는 기준 문서다.
>
> 화면에서 동일한 내용을 실시간으로 확인할 수 있다 — **13. 공통/플랫폼 → User Exit 관리 (COM-27)**.
> 그 화면은 `permStore`(COM-02 권한 매트릭스) 설정을 그대로 반영하므로, 권한을 바꾸면 표도 함께 바뀐다.
> 이 문서는 **기본 설정 기준 스냅샷**이며, 운영 중 변경된 권한은 COM-27 화면이 정본이다.

---

## 1. 권한 체계

### 1.1 권한그룹 (역할) — 9종

| 권한그룹 | 성격 | 담당 모듈 (기본 승인) |
| :--- | :--- | :--- |
| **관리자** | **admin** — 전 모듈 승인 | 전체 |
| 경영진 | 전 모듈 조회 전용 | 없음 (전체 조회) |
| 영업 | 기능 역할 | 01. 영업관리 (sd) |
| 구매 | 기능 역할 | 03. 구매관리 (mm) |
| 생산 | 기능 역할 | 02. SCM (scm), 05. 생산관리 (pp) |
| 품질 | 기능 역할 | 06. 품질관리 (qm) |
| 물류 | 기능 역할 | 04. 물류관리 (le) |
| 회계 | 기능 역할 | 08. 재무회계 (fi), 09. 관리회계 (co) |
| 연구소 | 기능 역할 | 07. 연구개발 (plm) |

- `role === "관리자"` 를 **admin** 으로 판정한다. admin 은 권한 매트릭스와 무관하게 전 모듈 승인이다.
- 기능 역할은 담당 모듈에 **승인**, 그 외 모듈에 **조회** 가 기본값이다.
- `status !== "활성"` 인 사용자(퇴사자 등)는 **모든 모듈이 "없음"** 이다. 역할과 무관하게 차단된다.

### 1.2 권한 등급 — 4단계

```
없음  <  조회  <  편집  <  승인
```

상위 등급은 하위를 포함한다. 등급별로 허용되는 동작은 다음과 같다.

| 등급 | 메뉴 표시 | 화면 진입 | 생성·수정·업로드 | 삭제·승인 |
| :--- | :---: | :---: | :---: | :---: |
| 없음 | ✕ | ✕ | ✕ | ✕ |
| 조회 | ○ | ○ | ✕ | ✕ |
| 편집 | ○ | ○ | ○ | ✕ |
| 승인 | ○ | ○ | ○ | ○ |

### 1.3 인가 집행 지점

| 지점 | 구현 | 비고 |
| :--- | :--- | :--- |
| 메뉴 숨김 | `Gnb.tsx` — 권한 "없음" 모듈을 GNB 에서 제외 | |
| 라우트 차단 | `ModuleGuard.tsx` — 주소창 직접 진입도 차단 | `ROUTE_GUARD_ENABLED` 로 끌 수 있다 |
| 액션 차단 | `MasterCrudPage` · `MassUpdateBar` · 문서 화면 | 버튼 비활성 + 이유 툴팁 |
| 서버측 재검증 | `server/authz.mjs` — 쓰기 요청 403 | 저장 키 → 모듈 매핑으로 판정 |

> **보안 한계 (반드시 인지)**
> 이 프로토타입은 **인증이 없다**. 사용자 전환은 화면에서 선택하는 방식이며 비밀번호를 다루지 않는다.
> 서버는 요청 헤더(`X-ERP-User-Role` 등)로 신원을 받으므로 **위조가 가능하다.**
> 따라서 서버측 재검증은 암호학적 보안 경계가 아니라 **클라이언트 우회로 인한 오조작 방지·감사** 목적이다.
> 운영 전환 시 세션/토큰 기반 인증으로 교체해야 한다.

---

## 2. User Exit (확장 훅)

### 2.1 개념

표준 로직 사이에 고객 정의 코드를 끼우는 지점이다(SAP User Exit 개념).
각 Exit 은 **필요한 역할×모듈 권한을 선언**하며, 현재 사용자가 그 권한을 충족하면
Exit 이 **가동**되고 충족하지 못하면 **바이패스**된다.

```
표준 로직 → [User Exit 지점] → 표준 로직 계속
                  ↓
        권한 충족?  → 예: Exit 실행 (통과 / 경고 / 차단)
                    → 아니오: 바이패스 (표준 로직만 수행)
```

- Exit 이 하나도 없거나 전부 바이패스되면 **기존과 같은 결과**가 나온다(하위 호환).
- Exit 이 예외를 던지면 **차단**한다. 조용히 넘기면 잘못된 저장이 남기 때문이다.
- Exit 은 코드로 등록한다 — `erp-portal/src/services/userExitDefaults.ts`.

### 2.2 기본 제공 Exit — 3종

| Exit ID | 명칭 | 훅 지점 | 요구 권한 | 판정 |
| :--- | :--- | :--- | :--- | :--- |
| `EXIT_SD_CREDIT_CHECK` | 여신한도 검사 | SD 수주 저장 전 | 재무회계(fi) **조회** | 한도 초과 → **차단** / 소진율 90%↑ → 경고 |
| `EXIT_MM_PO_LIMIT` | 발주 승인한도 검사 | MM 발주 저장 전 | 구매관리(mm) **편집** | 1억원 초과 시 관리자·구매는 경고 통과, 그 외 **차단** |
| `EXIT_FI_JV_BALANCE` | 전표 차대 평형 검사 | FI 전표 저장 전 | 재무회계(fi) **편집** | 차변≠대변 → **차단** |

> 여신한도 검사가 fi 권한을 요구하는 이유와 그 트레이드오프는 **§2.4 T-1** 에 정리했다.
> 요약하면, 권한이 없는 사용자에게는 검증이 생략되므로 통제가 느슨해진다.

### 2.3 권한그룹 × User Exit 가동 매트릭스 (기본 설정 기준)

| 권한그룹 | 여신한도 검사<br/>(fi 조회) | 발주 승인한도<br/>(mm 편집) | 전표 차대 평형<br/>(fi 편집) |
| :--- | :---: | :---: | :---: |
| **관리자** (admin) | 가동 (승인) | 가동 (승인) | 가동 (승인) |
| 경영진 | 가동 (조회) | 바이패스 (조회) | 바이패스 (조회) |
| 영업 | 가동 (조회) | 바이패스 (조회) | 바이패스 (조회) |
| 구매 | 가동 (조회) | **가동 (승인)** | 바이패스 (조회) |
| 생산 | 가동 (조회) | 바이패스 (조회) | 바이패스 (조회) |
| 품질 | 가동 (조회) | 바이패스 (조회) | 바이패스 (조회) |
| 물류 | 가동 (조회) | 바이패스 (조회) | 바이패스 (조회) |
| 회계 | **가동 (승인)** | 바이패스 (조회) | **가동 (승인)** |
| 연구소 | 가동 (조회) | 바이패스 (조회) | 바이패스 (조회) |

괄호 안은 해당 역할이 **보유한 등급**이다. `가동`은 Exit 요구 등급을 충족했다는 뜻이다.

**읽는 방법**
- 여신한도 검사는 fi **조회**만 요구하므로 기본 설정에서는 거의 모든 역할이 가동된다.
- 발주 승인한도는 mm **편집**을 요구하므로 **구매·관리자만** 가동된다. 다른 역할이 발주를 만들면
  한도 검사가 돌지 않는다 — 단, 애초에 mm 편집 권한이 없으면 발주 생성 버튼 자체가 비활성이다.
- COM-02 에서 권한을 "없음"으로 내리면 해당 Exit 은 바이패스로 바뀐다.

### 2.4 설계 트레이드오프 (⚠️ 일부 미결)

권한 기반 활성/바이패스 구조에는 통제 관점의 트레이드오프가 있다.
현재 구현은 확정된 사양을 그대로 따른 결과이며, 아래 T-1·T-4 는 **재검토가 필요한 미결 항목**이다.

#### T-1. 권한이 낮으면 검증도 함께 꺼진다 (⚠️ 미결)

`requires` 를 충족하지 못하면 Exit 이 바이패스되므로, **권한이 낮은 사용자에게 검증이 오히려 느슨해진다.**

| 사용자 | fi 권한 | 여신한도 검사 | 결과 |
| :--- | :--- | :--- | :--- |
| 회계 | 승인 | 가동 | 한도 초과 수주 **차단** |
| 영업 | 없음 | 바이패스 | 한도 초과 수주 **통과** |

일반적인 통제 설계는 권한이 **높을수록** 예외를 허용한다(승인권자가 한도를 넘길 수 있다).
현재 구조는 그 반대다 — 권한이 없으면 판정이 생략된다.

**설계 근거**: 여신 데이터(한도·사용액)는 재무 정보이므로 이를 읽을 권한이 없는 사용자에게는
판정 자체가 성립하지 않는다는 관점이다. 바이패스 사실은 COM-27 화면과 콘솔 경고로 드러낸다.

**완화 수단** (현재 적용 가능)
- 필수 검증 Exit 은 `requires` 를 **비워** 항상 활성으로 둔다.
- 또는 기본 seed 처럼 해당 역할에 `fi = 조회` 를 유지해 검사가 돌게 한다.
  (기본 설정은 이 방식이다 — 영업의 fi 가 조회이므로 여신 검사가 가동된다)

> **미결**: 검증 Exit 을 권한으로 켜고 끄는 것이 옳은지, 아니면 검증은 항상 돌고
> **예외 승인 권한**만 권한으로 판단해야 하는지. 후자가 일반적인 ERP 통제 방식이다.

#### T-2. Exit 차단은 우회할 수 없다

`EXIT_SD_CREDIT_CHECK` 가 한도 초과를 판정하면 저장이 중단되고 누구도 넘길 수 없다.
기존 SD 수주 화면은 `confirm` 으로 넘길 수 있었으므로 **동작이 강화된 변경**이다.
승인권자의 예외 처리 경로가 필요하면 Exit 에 역할 조건을 추가해야 한다
(`EXIT_MM_PO_LIMIT` 은 관리자·구매를 경고 통과로 처리하는 예다).

#### T-3. Exit 오류는 차단으로 처리한다 (fail-closed)

Exit 이 예외를 던지면 저장을 중단한다. 조용히 넘기면 검증되지 않은 문서가 남기 때문이다.
대가는 **Exit 하나의 버그가 해당 업무를 멈춘다**는 것이다. 운영에서는 Exit 배포 전 검증이 필요하다.

#### T-4. Exit 은 화면에서만 실행된다 (⚠️ 미결 — 가장 중요)

현재 `runUserExits` 는 **클라이언트에서만** 호출된다(`SalesOrderPage`, `PurchaseOrder`).
서버(`server/authz.mjs`)는 **모듈 권한만** 재검증하고 업무 규칙은 검사하지 않는다.

```
클라이언트 UI 경유    → 권한 검사 ○  Exit 검증 ○
REST API 직접 호출    → 권한 검사 ○  Exit 검증 ✕   ← 여신한도·승인한도·차대평형 우회
```

즉 **여신한도·발주 승인한도·전표 차대 평형은 UI 를 거치지 않으면 전혀 적용되지 않는다.**
인증이 없어 신원 위조가 가능한 것과 별개의 문제이며, 신원이 정확해도 우회된다.

> **미결**: 업무 규칙 검증을 서버로 옮길지, 옮긴다면 Exit 구현을 클라이언트·서버가
> 공유할지(현재 Exit 은 TypeScript, 서버는 `.mjs`). 프로토타입 범위에서 어디까지 할지 판단이 필요하다.

### 2.5 새 Exit 추가 절차

1. `services/userExit.ts` 의 `UserExitPoint` 에 훅 지점 문자열을 추가한다(새 지점이 필요한 경우).
2. `services/userExitDefaults.ts` 에 `registerUserExit({ id, label, point, requires, run })` 를 추가한다.
3. 표준 로직(화면 저장 핸들러)에서 해당 지점에 `runUserExits(point, ctx, authz.can)` 를 호출한다.
4. COM-27 화면과 이 문서의 매트릭스를 갱신한다.

---

## 3. 사용자 메뉴얼 작성 시 반영 사항

메뉴얼에는 다음을 포함한다.

1. **로그인·사용자 전환** — 하단 상태바에서 사용자를 선택한다. 비밀번호는 사용하지 않는다(프로토타입).
   배지로 `admin` / `user` / `비활성` 을 구분한다.
2. **권한이 없을 때 보이는 화면** — 메뉴에 나타나지 않으며, 주소로 직접 접근하면
   🔒 안내 화면이 뜨고 현재 보유 등급과 문의 경로(COM-02)를 알려준다.
3. **버튼이 비활성인 이유** — 마우스를 올리면 필요한 권한을 툴팁으로 안내한다.
   예: "삭제는 승인 권한이 필요합니다."
4. **저장 시 나타나는 검증 메시지** — User Exit 이 낸 메시지다. `⛔` 는 차단, `⚠️` 는 경고(계속 가능),
   `✅` 는 통과 알림이다.
5. **관리자 업무** — COM-01 사용자 관리, COM-02 권한 매트릭스, COM-27 User Exit 관리.
   권한 변경은 즉시 반영되며 재로그인이 필요 없다.
6. **검증이 돌지 않는 경우** — 권한이 낮으면 일부 검증(User Exit)이 생략된다(§2.4 T-1).
   COM-27 화면에서 현재 사용자 기준 가동/바이패스를 확인할 수 있다.
   메뉴얼에는 "권한에 따라 적용되는 검증이 달라진다"는 점을 명시한다.
7. **주의** — 서버 연동(REST) 모드에서는 권한 변경이 모든 클라이언트에 공유된다.
   로컬 저장 모드에서는 해당 브라우저에만 적용된다. 상태바 배지로 모드를 확인할 수 있다.

---

## 4. 관련 산출물

| 구분 | 위치 |
| :--- | :--- |
| 권한 판정 로직 | `erp-portal/src/services/authz.ts` |
| 세션(사용자 전환) | `erp-portal/src/services/session.ts` |
| User Exit 프레임워크 | `erp-portal/src/services/userExit.ts` |
| 기본 Exit 구현 | `erp-portal/src/services/userExitDefaults.ts` |
| 라우트 차단 | `erp-portal/src/components/layout/ModuleGuard.tsx` |
| 서버측 재검증 | `erp-portal/server/authz.mjs` |
| 검증 시나리오 | `erp-portal/scripts/authz-scenario.ts` |
| 화면 | COM-01 사용자 관리 · COM-02 권한 매트릭스 · **COM-27 User Exit 관리** |

## 5. 변경 이력

| 버전 | 일자 | 내용 |
| :--- | :--- | :--- |
| v1.0 | 2026-08-13 | 최초 작성 — 권한 체계, Exit 3종, 매트릭스, 메뉴얼 반영 사항 |
| v1.1 | 2026-08-13 | §2.4 설계 트레이드오프 추가 (T-1~T-4). T-1·T-4 는 미결로 표시 |

> 작성 기준일: 2026-08-13 · 문서 버전 **v1.1**
