# ERP ERD·데이터모델 v3.0 (구현 기준 역산)

> 2026-07-03 | erp-portal/src/data/mock/* 스키마 기준
> 아키텍처: 인메모리 EntityStore (useSyncExternalStore 구독) — REST API 전환 시 services/store.ts만 교체

## 1. Master Entity

### Material (품목) — materialStore
| 필드 | 타입 | 비고 |
|---|---|---|
| code (PK) | string | FG-/SF-/RM-/PK- 접두 체계 |
| name, type | string | 완제품/반제품/원자재/부자재 |
| uom | string | EA/KG/M/SET |
| price | number | 잎=구매단가, FG=판매가 |
| stock, safety | number | 현재고/안전재고 |
| status | string | 사용/중지 |

### BOM — bomStore
| 필드 | 비고 |
|---|---|
| parent (FK→Material) | 상위품목 |
| child (FK→Material) | 구성품목 |
| qty, uom | 소요량 — **다단계: parent가 child의 parent가 될 수 있음 (FG→SF→RM)** |

### Customer (고객) — customerStore
code(PK), name, country, currency, payTerm, **grade(A/B/C), creditLimit, creditUsed**, status

### Partner (거래처 통합) — partnerStore
code(PK), name, **type(고객/공급사)**, country, currency, payTerm, status

### Warehouse — warehouseStore
code(PK), name, plant, type(완/반/원/부/반품), status

### VendorEval — vendorEvalStore
code(PK→Partner), quality/delivery/price/response(0~100) — 종합·등급은 계산값

## 2. Transaction Entity

### SalesOrder — salesOrderStore
code(PK), customer(FK), orderDate, dueDate, status(등록→출하예약→출하완료/취소), **lines[]: {material, qty, price}**

### Quotation — quotationStore
code(PK), customer(FK), date, validTo, status(작성→승인), lines[]

### PurchaseRequest — prStore
code(PK), dept(FK→Budget), material(FK), qty, amount, reqDate, dueDate, status(승인대기→승인/반려)

### PurchaseOrder — poStore
code(PK), **pr(FK→PR, nullable)**, vendor(FK), material(FK), qty, price, orderDate, dueDate, status(발주→입고완료)

### WorkOrder — woStore
code(PK), material(FK), qty, startDate, dueDate, status(계획→진행→완료), **good, defect** (수율 산출)

### Lot — lotStore
code(PK, LOT-YYMM-SEQ), material(FK), qty(잔량), wh(FK), vendor(FK), date, status(**가용/소진/보류**)

### InventoryTx — txStore
type(입고/출고/이동), material, qty, from, to, **lot(FK), ref(FK→PO/SO/WO)** — Traceability 근거

### Inspection — inspStore
code(PK), **lot(FK)**, material, vendor, qty, sample, defects, result(대기→합격/불합격), date

### Journal — journalStore
code(PK), date, desc, **ref(FK→SO/PO)**, status(작성→전기), lines[]: {account, dr, cr} — Σdr=Σcr 제약

### AR / AP — arStore/apStore
code(PK), **ref(FK→SO/PO)**, customer|vendor(FK), amount, invoiceDate, dueDate, status(미수→수금완료 / 미지급→지급완료)

### MPS — mpsStore
material(FK, FG), month, forecast, plan

### Budget — budgetStore
dept(PK), budget, used

## 3. 관계도 (E2E 데이터 흐름)

```
MPS ─BOM전개→ 소요량 ─부족→ PR ─승인→ PO ─입고→ Lot + InventoryTx + Material.stock↑
                                              └→ Inspection ─불합격→ Lot.보류
                                              └→ AP, Journal(원재료/외상매입금)
MPS ─부족(SF)→ WorkOrder ─완료→ Material.stock (FG↑, 하위 백플러시↓) + Lot
Customer ─여신체크→ SalesOrder ─ATP체크(Material.stock−할당)→ 출하예약
  → 출고: Lot FIFO차감 + InventoryTx + Material.stock↓ → 출하완료
  → Journal(외상매출금/제품매출) + AR ─수금→ Customer.creditUsed↓
출하완료 lines × stdCost(BOM 롤업) → 손익분석 / computeKpis → EIS
```

## 4. 파생 계산 (저장하지 않는 값)
ATP, 표준원가(BOM 재귀 롤업 ×1.15), 실제원가(×투입/양품), 공급사 종합점수/등급, Cp/Cpk, KPI 6종, 예외 7규칙 — 전부 조회 시점 계산 (insights.ts / finance.ts).

## 5. RDB 전환 가이드 (마스터플랜 1→2단계)
- 각 store → PostgreSQL 테이블 1:1 매핑, lines[]는 자식 테이블(sales_order_line 등)로 정규화
- PK는 현행 code 체계 유지 + surrogate id 병행 권장
- 트랜잭션 필요 지점: 입고(LOT+재고+PO), 출고(FIFO+재고+SO), WO완료(백플러시+입고), 수금(AR+여신) — 현재 화면 핸들러 단위가 곧 트랜잭션 경계
