# 4.6 프로토타입 잔여과제 · 운영개발 이관목록 v1.1

작성 2026-08-14 · 대상 AX-ERP Portal 프로토타입

운영개발 단계로 넘어가기 전에 프로토타입에서 더 만들 것과, 운영개발 시점에
다룰 것을 나눈다. 판단 기준은 **"프로토타입의 완료 기준(핵심 E2E 업무 흐름이
화면과 서비스 계층에서 동작)을 막는가"** 하나다. 막으면 Critical, 아니면 이관한다.

---

## 1. 현재 상태 요약

| 항목 | 수치 |
|---|---|
| 표준 메뉴 | 153 |
| 실화면 구현 | **138** (90.2%) |
| 미구현 | 15 |
| **메뉴 라벨 ↔ 화면 정합** | **138 일치 · 0 불일치** |
| 자동 검증 | 시나리오 9종 **253건** 통과 |
| 저장 키 | 33 (REST/localStorage 이중 모드) |

13개 모듈 중 **9개가 100% 구현**됐다 (sd·qm·plm·fi·co·mk·sv·mdm·com).
미구현 15개는 mm 6 · le 3 · pp 4 · scm 1 · com 1 에 몰려 있다.

> **점검 중 발견 (2026-08-14, 해소 완료)** — 메뉴 라벨과 실제 화면이
> **137개 중 56개(41%) 어긋나 있었다.** "출하관리"를 누르면 "수주 잔고"가
> 떴다. `menu.ts` 는 초기 커밋 이후 무변경이고 표준메뉴 문서 1.0 과 정확히
> 일치하는 반면, 화면들이 다른 목록으로 만들어져 드리프트했다.
> 화면을 정본으로 두고 메뉴 라벨 80개를 화면 타이틀로 맞췄다. 상세는 §3.1.

---

## 2. 인계 사항 — User Exit 작업에서 남긴 한계 2건

`4.5_권한체계_UserExit_매트릭스_v1.md` v1.2 §2.4 의 T-4 잔여분이다.
**둘 다 프로토타입의 의도된 범위 밖**이며, 운영개발에서 해소해야 한다.

### H-1. 여신한도는 서버가 검증하지 않는다

```
UI 경유            → 권한 ○  전표 차대 ○  발주 한도 ○  여신한도 ○
REST API 직접 호출 → 권한 ○  전표 차대 ○  발주 한도 ○  여신한도 ✕
```

- **원인** — 전표 차대·발주 한도는 문서 하나로 판정이 끝나는 *자기완결* 규칙이라
  `shared/businessRules.mjs` 의 순수 함수로 양쪽이 공유한다. 여신한도는
  고객 마스터의 여신 한도·기사용액을 **다른 저장 키에서 조회**해야 해서 순수 함수로
  만들 수 없다.
- **기술적으로는 지금도 가능하다** — 서버 `storage.read("sales.customer")` 로 읽으면
  된다. 다만 규칙 함수가 저장소에 의존하기 시작하면 클라이언트와 공유가 깨지고,
  키 간 참조·일관성 문제를 프로토타입 JSON 저장소에서 떠안게 된다.
  grill-me 에서 **"자기완결 규칙만 서버 검증"** 으로 범위를 확정한 근거다.
- **해소 시점** — 실 DB 전환과 동시. 트랜잭션 안에서 고객 여신을 조회·잠글 수 있게
  되면 자연스럽게 풀린다. → D-2 에 묶는다.
- **그때까지의 통제** — UI 를 거치는 한 여신한도는 정상 작동한다. API 직접 호출은
  개발·테스트 경로이며, 운영 노출 전에 D-1 이 먼저 닫힌다.

### H-2. 인증이 없어 신원 헤더는 위조할 수 있다

- `X-ERP-User-Id/Role/Status` 헤더로 신원이 오고, 서버는 이를 그대로 믿는다.
  **암호학적 보안 경계가 아니다.** 목적은 (1) 클라이언트 우회 오조작 방지
  (2) 감사 흔적이며, `server/authz.mjs` 상단 주석에 명시돼 있다.
- 헤더가 아예 없으면 인가 검사를 건너뛴다(스크립트·테스트 호환). 업무 규칙은
  헤더가 없어도 항상 적용된다 — 시나리오로 고정해 뒀다.
- **해소 시점** — 운영개발 착수 즉시. → D-1.

> **이관 시 반드시 전달할 것**: 위 두 가지는 *버그가 아니라 명시적 설계 결정*이다.
> 운영개발자가 "왜 서버가 여신한도를 안 막지?" 로 시간을 쓰지 않도록,
> 이 문서와 `4.5` §2.4 를 함께 넘긴다.

---

## 3. Critical — 처리 완료 (2026-08-14)

세 건 모두 처리했다. C-1·C-3 은 착수 후 §3.1 이 드러나면서 **범위가 바뀌었다.**

### 3.1 (선행) 메뉴 라벨 ↔ 화면 정합성 복구

점검 중 발견해 C-1·C-3 보다 먼저 처리했다. **이것이 셋 중 가장 큰 결함이었다.**

- **증상** — 라우트가 있는 137개 중 **56개(41%)** 에서 메뉴 라벨과 화면이 달랐다.
  `출하관리` → `수주 잔고`, `원가센터` → `원가요소마스터`, `조직마스터` → `작업장마스터` …
- **원인** — `menu.ts` 는 초기 커밋 이후 한 번도 바뀌지 않았고 표준메뉴 문서 1.0 과
  정확히 일치한다. 화면들이 그 뒤 **다른 목록으로 만들어지며 드리프트**했다.
  각 화면은 자기 슬러그와 맞는 코드(`sd-06` → `SD-006`)를 달고 있어 내부적으로는 일관됐다.
- **판정** — **화면을 정본**으로 두고 메뉴 라벨 80개를 화면 타이틀로 교체했다.
  반대로 화면을 스펙에 재바인딩하면 스펙에 슬롯이 없는 화면(원가요소마스터·
  공급사 포털·VOC관리·계측기관리 등)이 메뉴에서 사라진다.
- **결과** — **138 일치 · 0 불일치.** 변경은 `menu.ts` 한 파일이라 되돌리기 쉽다.
- **비용** — 표준메뉴 문서 1.0 및 4.1·4.2 는 이제 구현과 다르다. 역산 갱신이 필요하다.

### 3.2 C-3. 중복 바인딩 해소

§3.1 로 라벨이 정리되자 **진짜 중복이 드러났다.** 당초 지목한 중복
(`mm-01`↔`mdm-04`, `mm-13`↔`le-11`)은 드리프트한 라벨 때문에 생긴 착시였다.

| 실제 중복 | 처리 |
|---|---|
| `CommonCodeMaster` 가 `com-06` · `mdm-12` 두 곳에 | 기준정보 성격이므로 **mdm-12 를 정본**으로. `com-06`(API Gateway)은 미구현으로 되돌림 |
| `ProfitAnalysis` 가 `co-11` · `co-12` 두 곳에 | **co-11 을 정본**으로. `co-12` 는 본래 이름대로 경영 Dashboard 를 신규 구축(§3.3) |

### 3.3 C-1. 모듈 대시보드 — 5개 중 3개 구축

§3.1 이후 실제 대시보드 보유 현황이 드러났다. 기존 대시보드는
`pp-01 생산 종합대시보드`, `scm-07 SCM Control Tower` **2개뿐**이었다.

| 슬러그 | 판단 |
|---|---|
| **mm-15 구매 Dashboard** | ✅ 신규 — 발주·PR·예산·공급사 평가 집계 |
| **le-14 물류 Dashboard** | ✅ 신규 — 입출고·창고별 LOT·안전재고·수불 |
| **co-12 경영 Dashboard** | ✅ 신규 — 전사 수익성·자산·품질 KPI + 전사 예외 |
| ~~scm-09 SCM Dashboard~~ | ❌ **만들지 않음** — `scm-07 SCM Control Tower`(수요·공급·재고·리스크 통합 관제)와 겹친다 |
| ~~pp-14 생산 Dashboard~~ | ❌ **만들지 않음** — `pp-01 생산 종합대시보드`(OEE·달성률·양품실적)와 겹친다 |

방금 §3.2 에서 중복 바인딩을 없애 놓고 같은 것을 새로 만들 이유가 없다.
두 건은 §4 이관 목록에서도 **제외**한다 — 필요해지면 기존 화면을 확장한다.

세 화면 모두 **새 store 를 만들지 않았다.** 각 모듈 화면이 이미 쓰는 store 와
`insights.computeKpis` / `computeExceptions` 를 그대로 집계한다.

### 3.4 C-2. 인증 접점(seam) 확정

접점이 이미 세 곳으로 잘 격리돼 있어 **주석만 추가했다. 동작 변경 없음.**
`grep -rn "AUTH-SEAM"` 으로 전부 찾을 수 있다. 교체 절차는 §5.1.

---

## 4. 이관 — 운영개발 단계에서, 시점에 맞춰 제안할 것

각 항목의 **제안 시점**을 명시한다. 그 시점이 오면 다시 제안한다.

### 4.1 착수 즉시 (운영개발 1주차)

| ID | 항목 | 사유 |
|---|---|---|
| **D-1** | 인증·SSO 실구현 (OIDC/세션) | H-2 해소. 다른 모든 보안 항목의 전제 |
| **D-2** | PostgreSQL 전환 + 트랜잭션 | H-1 해소를 포함. 저장소 교체는 늦을수록 비싸다 |

D-1 과 D-2 는 **묶어서** 다룬다. 인증이 세션 저장소를, 여신한도가 트랜잭션을
필요로 해서 서로 맞물린다.

### 4.2 데이터 모델 확정 후

| ID | 항목 | 제안 시점 |
|---|---|---|
| D-3 | 감사 로그 영속화 (누가·언제·무엇을) | D-2 직후. 테이블 설계에 포함 |
| D-4 | 첨부파일 (도면·계약서·검사성적서) | 스키마 확정 시. 저장소 선택 동반 |
| D-5 | 결재선·워크플로 엔진 | 승인 요건 확정 시. 현재는 권한 등급만 |

### 4.3 실데이터 규모 확인 후

| ID | 항목 | 제안 시점 |
|---|---|---|
| D-6 | 서버 페이지네이션·필터링·정렬 | 단일 테이블 1만 건 초과가 보일 때 |
| D-7 | 스냅샷 부트스트랩 분할 로딩 | 현재 155개 스토어를 1 GET 으로 받는다. 초기 로딩이 체감될 때 |

지금은 합성 데이터라 전량 로딩이 가장 단순하고 빠르다. **미리 나누지 않는다.**

### 4.4 요건 확정 시

| ID | 항목 | 제안 시점 |
|---|---|---|
| D-8 | 다국어(i18n) | 해외 법인·영문 사용자 요건이 확정될 때 |
| D-9 | 미구현 업무화면 9개 | 해당 업무 범위가 스코프에 들어올 때 |

**D-9 상세** — 미구현 15개 중, §3.3 에서 제외한 2개(`scm-09`·`pp-14`)를 뺀 **13개**:

| 모듈 | 화면 | 성격 |
|---|---|---|
| mm | mm-01 공급업체관리 | `mdm-04 공급사마스터` 가 이미 있다. **구매 관점 뷰가 따로 필요한지 먼저 판단** |
| mm | mm-10 설비 구매 · mm-11 금형 구매 | `mm-08 원자재 구매` 와 동일 패턴 반복 · 저위험 |
| mm | mm-12 통관관리 · mm-13 수입관리 | `le-10 수출입 및 통관관리` 와 겹칠 소지. **범위 확정 후 착수** |
| mm | mm-14 구매실적관리 | 신규 업무 |
| le | le-11 수입관리 · le-12 보험관리 · le-13 클레임관리 | 신규 업무 (le-11 은 mm-13 과 중복 소지) |
| pp | pp-11 생산마감 | 신규 업무. 회계기간 마감과 연동 검토 |
| pp | pp-12 설비가동현황 · pp-13 OEE 분석 | 설비 데이터 수집(MES 연계) 전제 |
| com | com-06 API Gateway | §3.2 에서 중복 바인딩을 해제해 미구현으로 되돌린 자리 |

- **pp-12·13 은 설비 실적 데이터 소스가 없으면 의미가 없다.** MES 연계 여부가
  정해지기 전에 만들면 합성 데이터 화면만 남는다. 연계 결정 후 제안한다.
- **mm-12·13 ↔ le-10·11 은 착수 전에 소유 모듈을 정한다.** §3.2 에서 겪은
  중복 바인딩을 반복하지 않기 위해서다.

---

## 5. 운영 전환 시 교체 지점

### 5.1 인증 접점 (C-2 산출물)

코드에 `AUTH-SEAM n/3` 주석으로 고정했다. `grep -rn "AUTH-SEAM"` 으로 전부 찾을 수 있다.

| # | 역할 | 위치 | 교체 내용 |
|---|---|---|---|
| **1/3** | 신원의 **출처** | `src/services/session.ts` | `currentUserId` → 토큰 subject, `setCurrentUserId` → 로그인/로그아웃. 화면은 `useCurrentUser()` 만 쓰므로 **호출부 무변경** |
| **2/3** | 신원의 **전송** | `src/services/restBackend.ts` `identityHeaders()` | `X-ERP-User-*` → `Authorization: Bearer <token>` |
| **3/3** | 신원의 **검증** | `server/authz.mjs` `identityFromHeaders()` | 헤더 파싱 → 토큰 서명 검증. **여기가 진짜 신뢰 경계가 될 자리다** |

**교체 절차** — 3 → 2 → 1 순서로 한다. 서버가 먼저 토큰을 받을 수 있어야
클라이언트를 바꿔도 중단이 없다.

1. **3/3**: `identityFromHeaders` 를 토큰 검증으로 교체하고 실패 시 **401**.
   반환 형태 `{id, role, status}` 를 유지하면 `checkAuthz` 와
   `index.mjs` 의 `canApproveModule` 은 **손대지 않아도 된다.**
   동시에 `checkAuthz` 의 **"헤더 없으면 통과" 분기를 제거**한다(테스트 호환용이었다).
2. **2/3**: 헤더를 토큰으로 교체.
3. **1/3**: 세션을 IdP 연동으로 교체하고 사용자 전환 UI 를 제거한다.

**함께 처리해야 할 것 2가지**

- **GET 요청에 토큰이 실리지 않는다.** 지금은 서버가 읽기를 항상 허용해서
  스냅샷·재조회 fetch 가 신원 헤더를 달지 않는다. 인증 도입 시 `jsonInit`
  뿐 아니라 `restBackend.ts` 의 GET fetch 들에도 함께 적용해야 한다.
  놓치면 **읽기만 무인증으로 열린다.**
- **시나리오 스크립트 9종이 헤더로 신원을 흉내낸다.** 401 을 도입하면
  테스트 픽스처에 토큰 발급이 필요하다. 특히 `authz-scenario.ts` §5-2.

### 5.2 저장소·기타

`CLAUDE.md` 의 보안 제약을 코드 위치로 옮긴 것이다.

| 현재 | 교체 대상 | 파일 |
|---|---|---|
| 헤더 기반 신원 | 세션·토큰 검증 | `server/authz.mjs` `identityFromHeaders` |
| 헤더 주입 | 토큰 주입 | `src/services/restBackend.ts` |
| 브라우저 로컬 세션 | IdP 연동 | `src/services/session.ts` |
| JSON 파일 저장소 | 트랜잭션 DB | `server/storage.mjs` |
| 낙관적 쓰기 + 롤백 | 서버 확정 후 반영 재검토 | `src/services/store.ts` |
| 클라이언트 여신 검증 | 서버 트랜잭션 내 검증 | `shared/businessRules.mjs` |

`EntityStore` 의 `getAll()` 동기 · `create()` 동기 반환 계약 때문에 낙관적 쓰기가
**구조적으로 강제**된다. 서버 확정 후 반영으로 바꾸려면 이 계약부터 바꿔야 한다.
전환 시 가장 먼저 검토할 지점이다.

---

## 6. 권고

1. ~~C-1 · C-2 · C-3 을 프로토타입 마지막 작업으로 처리한다~~ → **완료**(§3).
   착수 후 메뉴 정합성 결함이 드러나 C-1·C-3 의 범위가 바뀌었다.
2. **문서 1.0 · 4.1 · 4.2 를 구현 기준으로 역산 갱신한다.** §3.1 이 남긴 유일한
   미결 비용이다. 지금 메뉴는 구현과 일치하지만 문서는 아직 옛 스펙이다.
3. **D-1 · D-2 를 묶어 운영개발 1주차에 놓는다.** H-1·H-2 가 여기서 함께 닫힌다.
4. 나머지는 **시점이 오기 전에 만들지 않는다.** 특히 D-6·D-7 은 실측 없이
   최적화하면 단순한 구조를 잃는다.
5. **화면을 새로 만들기 전에 기존 화면과 겹치는지 먼저 본다.** §3.2·§3.3 에서
   중복 3건(바인딩 2 · 대시보드 2)이 나왔다. 이 프로젝트에서 반복되는 실패 양상이다.

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1 | 2026-08-14 | 최초 작성. 잔여 한계 2건 인계, Critical 3건 / 이관 9건 분류 |
| v1.1 | 2026-08-14 | Critical 3건 처리 완료. 메뉴 정합성 결함(56/137) 발견·복구, 중복 바인딩 2건 해소, 대시보드 3건 신규(2건은 중복이라 제외), 인증 접점 `AUTH-SEAM` 고정 |
