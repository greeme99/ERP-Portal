# 4.6 프로토타입 잔여과제 · 운영개발 이관목록 v1

작성 2026-08-14 · 대상 AX-ERP Portal 프로토타입 · 근거 커밋 `8746e27`

운영개발 단계로 넘어가기 전에 프로토타입에서 더 만들 것과, 운영개발 시점에
다룰 것을 나눈다. 판단 기준은 **"프로토타입의 완료 기준(핵심 E2E 업무 흐름이
화면과 서비스 계층에서 동작)을 막는가"** 하나다. 막으면 Critical, 아니면 이관한다.

---

## 1. 현재 상태 요약

| 항목 | 수치 |
|---|---|
| 표준 메뉴 | 153 |
| 실화면 구현 | **137** (89.5%) |
| 미구현 | 16 |
| 라우트 등록 | 164 (상세·서브 화면 포함) |
| 자동 검증 | 시나리오 9종 **253건** 통과 |
| 저장 키 | 33 (REST/localStorage 이중 모드) |

13개 모듈 중 **9개가 100% 구현**됐다 (sd·qm·plm·fi·co·mk·sv·mdm·com).
미구현 16개는 mm 7 · le 4 · pp 4 · scm 1 에 몰려 있다.

---

## 2. 인계 사항 — User Exit 작업에서 남긴 한계 2건

`4.5_권한체계_UserExit_매트릭스_v1.md` v1.2 §2.4 의 T-4 잔여분이다.
**둘 다 프로토타입의 의도된 범위 밖**이며, 운영개발에서 해소해야 한다.

### H-1. 여신한도는 서버가 검증하지 않는다

```
UI 경유            → 권한 ○  전표 차대 ○  발주 한도 ○  여신한도 ○
REST API 직접 호출 → 권한 ○  전표 차대 ○  발주 한도 ○  여신한도 ✕
```

- **원인** — 전표 차대·발주 한도는 문서 하나로 판정이 끝나는 *자기완결* 규칙이라
  `shared/businessRules.mjs` 의 순수 함수로 양쪽이 공유한다. 여신한도는
  고객 마스터의 여신 한도·기사용액을 **다른 저장 키에서 조회**해야 해서 순수 함수로
  만들 수 없다.
- **기술적으로는 지금도 가능하다** — 서버 `storage.read("sales.customer")` 로 읽으면
  된다. 다만 규칙 함수가 저장소에 의존하기 시작하면 클라이언트와 공유가 깨지고,
  키 간 참조·일관성 문제를 프로토타입 JSON 저장소에서 떠안게 된다.
  grill-me 에서 **"자기완결 규칙만 서버 검증"** 으로 범위를 확정한 근거다.
- **해소 시점** — 실 DB 전환과 동시. 트랜잭션 안에서 고객 여신을 조회·잠글 수 있게
  되면 자연스럽게 풀린다. → D-2 에 묶는다.
- **그때까지의 통제** — UI 를 거치는 한 여신한도는 정상 작동한다. API 직접 호출은
  개발·테스트 경로이며, 운영 노출 전에 D-1 이 먼저 닫힌다.

### H-2. 인증이 없어 신원 헤더는 위조할 수 있다

- `X-ERP-User-Id/Role/Status` 헤더로 신원이 오고, 서버는 이를 그대로 믿는다.
  **암호학적 보안 경계가 아니다.** 목적은 (1) 클라이언트 우회 오조작 방지
  (2) 감사 흔적이며, `server/authz.mjs` 상단 주석에 명시돼 있다.
- 헤더가 아예 없으면 인가 검사를 건너뛴다(스크립트·테스트 호환). 업무 규칙은
  헤더가 없어도 항상 적용된다 — 시나리오로 고정해 뒀다.
- **해소 시점** — 운영개발 착수 즉시. → D-1.

> **이관 시 반드시 전달할 것**: 위 두 가지는 *버그가 아니라 명시적 설계 결정*이다.
> 운영개발자가 "왜 서버가 여신한도를 안 막지?" 로 시간을 쓰지 않도록,
> 이 문서와 `4.5` §2.4 를 함께 넘긴다.

---

## 3. Critical — 프로토타입에서 처리할 것

### C-1. 모듈 대시보드 4개 누락 (scm-09 · mm-15 · le-14 · pp-14)

주요 사용자에 **경영진**이 들어 있고 대시보드가 그 진입점인데, 13개 모듈 중
4개만 대시보드가 없어 일관성이 깨진다. 다른 모듈에 이미 대시보드 패턴이 있어
**기존 구조 재사용**이며 새 개념이 없다.

- 위험 낮음 · 새 의존성 없음 · `insights.ts` 의 KPI 계산을 그대로 쓴다.

### C-2. 인증 접점(seam) 확정

인증 *구현* 은 이관 대상이지만, **어디에 무엇을 끼울지는 지금 정해야** 한다.
프로토타입이 끝난 뒤에 정하면 세션·헤더·서버 판정 세 군데를 다시 헤집게 된다.

- `services/session.ts` 가 유일한 신원 출처인지 확인하고, IdP 로 교체할 지점을 고정
- `restBackend.ts` 의 헤더 주입부를 **토큰 주입부로 바꿀 자리**로 주석 명시
- `server/authz.mjs` 의 `identityFromHeaders` 를 세션 검증으로 교체하는 경계 명시
- 산출물은 **코드 주석 + 이 문서 §5 의 교체 절차**다. 동작 변경 없음.

### C-3. 중복 메뉴 판정 (mm-01 ↔ mdm-04, mm-13 ↔ le-11)

| 중복 | 판단 필요 |
|---|---|
| `mm-01 공급업체관리` ↔ `mdm-04 공급사마스터` (구현됨) | 같은 대상인가, 구매 관점 뷰인가 |
| `mm-13 수입관리` ↔ `le-11 수입관리` (둘 다 미구현) | 한쪽으로 합칠 것인가 |

미구현 16개 중 3개가 여기 걸려 있다. **판정 없이 만들면 중복 구현**이 되므로
개발보다 결정이 먼저다. 결정만 하면 §4 의 물량이 줄어든다.

---

## 4. 이관 — 운영개발 단계에서, 시점에 맞춰 제안할 것

각 항목의 **제안 시점**을 명시한다. 그 시점이 오면 다시 제안한다.

### 4.1 착수 즉시 (운영개발 1주차)

| ID | 항목 | 사유 |
|---|---|---|
| **D-1** | 인증·SSO 실구현 (OIDC/세션) | H-2 해소. 다른 모든 보안 항목의 전제 |
| **D-2** | PostgreSQL 전환 + 트랜잭션 | H-1 해소를 포함. 저장소 교체는 늦을수록 비싸다 |

D-1 과 D-2 는 **묶어서** 다룬다. 인증이 세션 저장소를, 여신한도가 트랜잭션을
필요로 해서 서로 맞물린다.

### 4.2 데이터 모델 확정 후

| ID | 항목 | 제안 시점 |
|---|---|---|
| D-3 | 감사 로그 영속화 (누가·언제·무엇을) | D-2 직후. 테이블 설계에 포함 |
| D-4 | 첨부파일 (도면·계약서·검사성적서) | 스키마 확정 시. 저장소 선택 동반 |
| D-5 | 결재선·워크플로 엔진 | 승인 요건 확정 시. 현재는 권한 등급만 |

### 4.3 실데이터 규모 확인 후

| ID | 항목 | 제안 시점 |
|---|---|---|
| D-6 | 서버 페이지네이션·필터링·정렬 | 단일 테이블 1만 건 초과가 보일 때 |
| D-7 | 스냅샷 부트스트랩 분할 로딩 | 현재 155개 스토어를 1 GET 으로 받는다. 초기 로딩이 체감될 때 |

지금은 합성 데이터라 전량 로딩이 가장 단순하고 빠르다. **미리 나누지 않는다.**

### 4.4 요건 확정 시

| ID | 항목 | 제안 시점 |
|---|---|---|
| D-8 | 다국어(i18n) | 해외 법인·영문 사용자 요건이 확정될 때 |
| D-9 | 미구현 업무화면 9개 | 해당 업무 범위가 스코프에 들어올 때 |

**D-9 상세** (C-3 중복 판정 후 남는 것):

| 모듈 | 화면 | 성격 |
|---|---|---|
| mm | mm-10 설비 구매 · mm-11 금형 구매 | mm-08/09 와 동일 패턴 반복 · 저위험 |
| mm | mm-12 통관관리 · mm-14 구매실적관리 | 신규 업무 |
| le | le-12 보험관리 · le-13 클레임관리 | 신규 업무 |
| pp | pp-11 생산마감 · pp-12 설비가동현황 · pp-13 OEE 분석 | 설비 데이터 수집(MES 연계) 전제 |

pp-12·13 은 **설비 실적 데이터 소스가 없으면 의미가 없다.** MES 연계 여부가
정해지기 전에 만들면 합성 데이터 화면만 남는다. 연계 결정 후 제안한다.

---

## 5. 운영 전환 시 교체 지점

`CLAUDE.md` 의 보안 제약을 코드 위치로 옮긴 것이다.

| 현재 | 교체 대상 | 파일 |
|---|---|---|
| 헤더 기반 신원 | 세션·토큰 검증 | `server/authz.mjs` `identityFromHeaders` |
| 헤더 주입 | 토큰 주입 | `src/services/restBackend.ts` |
| 브라우저 로컬 세션 | IdP 연동 | `src/services/session.ts` |
| JSON 파일 저장소 | 트랜잭션 DB | `server/storage.mjs` |
| 낙관적 쓰기 + 롤백 | 서버 확정 후 반영 재검토 | `src/services/store.ts` |
| 클라이언트 여신 검증 | 서버 트랜잭션 내 검증 | `shared/businessRules.mjs` |

`EntityStore` 의 `getAll()` 동기 · `create()` 동기 반환 계약 때문에 낙관적 쓰기가
**구조적으로 강제**된다. 서버 확정 후 반영으로 바꾸려면 이 계약부터 바꿔야 한다.
전환 시 가장 먼저 검토할 지점이다.

---

## 6. 권고

1. **C-1 · C-2 · C-3 을 프로토타입 마지막 작업으로 처리한다.** 셋 다 새 개념이 없고,
   C-2·C-3 은 개발보다 결정에 가깝다.
2. **D-1 · D-2 를 묶어 운영개발 1주차에 놓는다.** H-1·H-2 가 여기서 함께 닫힌다.
3. 나머지는 **시점이 오기 전에 만들지 않는다.** 특히 D-6·D-7 은 실측 없이
   최적화하면 단순한 구조를 잃는다.

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1 | 2026-08-14 | 최초 작성. 잔여 한계 2건 인계, Critical 3건 / 이관 9건 분류 |
